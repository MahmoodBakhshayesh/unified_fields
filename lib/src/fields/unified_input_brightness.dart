/// Explicit brightness for unified inputs when [ThemeData.brightness] should be overridden.
enum UnifiedInputBrightness {
  /// Use the light palette.
  light,

  /// Use the dark palette.
  dark,
}
